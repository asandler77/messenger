import React, { ReactElement } from 'react';
import { Text, View } from 'react-native';

export const EntryPoint = (): ReactElement => {
  return (
    <View>
      <Text>Hello!!!</Text>
    </View>
  );
};
